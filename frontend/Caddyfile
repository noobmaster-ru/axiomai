{
    admin off
    email {$ACME_EMAIL}
}

{$APP_DOMAIN:localhost} {
    encode zstd gzip

    @api path /api/*
    handle_path /api/* {
        reverse_proxy api:8000
    }

    handle {
        root * /srv
        try_files {path} /index.html
        file_server
    }
}
